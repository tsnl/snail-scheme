#pragma once

#define CONFIG_SIZEOF_VOID_P    (8)
#define CONFIG_DEBUG_MODE       (1)

// debug configs: enable/disable to turn on/off specific debug features
#define CONFIG_DISABLE_RUNTIME_TYPE_CHECKS          (0)
#define CONFIG_PRINT_EACH_INSTRUCTION_ON_EXECUTION  (0)
#define CONFIG_DUMP_VM_STATE_AFTER_EXECUTION        (0)
